# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: silverbridge-web
services:  
  app:
    language: dotnet
    project: ./src/SilverbridgeWeb.AppHost/SilverbridgeWeb.AppHost.csproj
    host: containerapp
