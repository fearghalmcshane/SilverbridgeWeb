@page "/contact"

<PageTitle>Contact Us</PageTitle>

<MudContainer MaxWidth="MaxWidth.Medium">
    <MudGrid>
        <MudItem xs="12">
            <MudText Typo="Typo.h3" Class="px-4">Contact Us</MudText>
        </MudItem>
        <MudItem xs="12" class="mt-4">
            <MudText Typo="Typo.body1" Class="px-4">
                If you have any questions or inquiries, please fill out the form below and we will get back to you as soon as possible.
            </MudText>
        </MudItem>
        <MudItem xs="12" class="mt-4">
            @* <MudForm @ref="form" @valid="HandleValidSubmit"> *@
            @*     <MudTextField @bind-Value="name" Label="Name" Required="true" RequiredError="Name is required" /> *@
            @*     <MudTextField @bind-Value="email" Label="Email" Required="true" RequiredError="Email is required" /> *@
            @*     <MudTextField @bind-Value="message" Label="Message" Required="true" RequiredError="Message is required" Lines="4" /> *@
            @*     <MudButton Type="Submit" Variant="Variant.Filled" Color="Color.Primary" Class="mt-4">Submit</MudButton> *@
            @* </MudForm> *@
        </MudItem>
    </MudGrid>
</MudContainer>

@code {
    private MudForm form;
    private string name;
    private string email;
    private string message;

    private void HandleValidSubmit()
    {
        // Handle form submission logic here
        Console.WriteLine($"Name: {name}, Email: {email}, Message: {message}");
    }
}
